# fetch 更多边界测试设计

## 目标
- 在不改业务逻辑的前提下，继续补齐 `fetch` 的网络边界测试。

## 本轮范围
- 多跳重定向后的最终 URL 与相对 JS 跳转解析
- 错误 `deflate` 头的容错行为
- 缺失 `Content-Type` 时的保守处理
- `204 No Content` 边界

## 暂不纳入
- 真正的 `br` 解码支持：这很可能需要修改业务逻辑，先不动
- 更重的代理/TLS 集成：成本较高，后续再补
